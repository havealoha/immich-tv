{
  "name": "immich-tv-marketing-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "firebase:serve": "firebase emulators:start --only hosting --config firebase.json",
    "firebase:deploy": "firebase deploy --only hosting --config firebase.json"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react-swc": "^3.8.1",
    "firebase-tools": "^14.4.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  }
}
